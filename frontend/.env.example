# Frontend env example
# Do not bake the admin token into the frontend bundle.
# The login page stores the token in browser localStorage after a successful login.

# Default uses the Vite proxy. Change this to the backend /api URL when deploying separately.
VITE_API_BASE_URL=/api
